# Zenmap (GTK 3) — Python dependencies for source installs and virtualenvs.
#
# GTK 3, GObject introspection data, and Pango/Cairo stack must come from the
# OS or distro packages (not reliably installable from PyPI alone). Examples:
#   Debian/Ubuntu: python3-gi python3-gi-cairo gir1.2-gtk-3.0
#   Fedora: python3-gobject gtk3
#   macOS: often via Homebrew gtk+3 pygobject3
#
# PyGObject on PyPI is useful when building in a venv alongside system typelibs:
PyGObject>=3.36
